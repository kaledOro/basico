#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
	 
}
void declararInteiro()
{
	char Int[18], nomeInt[10];
	fflush(stdin);
	gets(Int);
	fflush(stdin);
	gets(nomeInt);
	if (strcmp(Int, "declararInteiro")==0)
	{
		int nomeInt1;//nomeIunt
	}
	int main();
}
void mostrarInteiro()
{
	char mostrarInteiro[15], MostrarNome[10];
	fflush(stdin);
	gets(mostrarInteiro);
	fflush(stdin);
	gets(MostrarNome);
	if (strcmp(mostrarInteiro, "mostrarInteiro")==0)
	{
		if (strcmp(MostrarNome, nomeInt)==0)
		printf("%d", nomeInt);
		
	}
}